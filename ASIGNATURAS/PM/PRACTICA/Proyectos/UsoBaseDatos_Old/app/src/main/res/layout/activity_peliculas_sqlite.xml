<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SQLitePeliculasActivity">

    <!-- ImageView para el fondo -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/cartel_jaws"
        android:scaleType="fitXY"
        android:contentDescription="@string/content_description_background" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/jawstoolbar"
        style="@style/AppTheme.AppBarOverlay"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/verdeazulito">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_menu_movie_light"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/content_description_menu_icon" />

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mi cine favorito"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/content_description_toolbar_title"
            tools:ignore="VisualLintBounds" />

        <!--<ImageButton
          android:id="@+id/toolbar_button"
          android:layout_width="32dp"
          android:layout_height="32dp"
          android:src="@drawable/lightbulb24dp"
          android:background="?attr/selectableItemBackground"
          android:contentDescription="@string/button_description"
          android:layout_gravity="end|center_vertical"
          android:layout_marginEnd="16dp" /> -->

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ico_surfing_white_24dp"
            android:contentDescription="@string/descripcion_icono_toolbar"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="16dp" />

        <!--<TextView
            android:id="@+id/toolbar_counter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="16dp"
            android:text="@string/contador"
            android:textColor="#000000"
            android:textSize="14sp" /> -->

    </androidx.appcompat.widget.Toolbar>

</RelativeLayout>
